server:
  port: 9191
  
spring:
  application:
    name: PRODUCTION-SERVICE
    
ribbon:
  eureka:
    enabled: true
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
      hostname: localhost

#management:
#  security:
#    enabled: false
